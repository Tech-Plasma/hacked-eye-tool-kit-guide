const [base, setBase] = useUpload() 
const [downlode, forceDownlode] = use_Manipulate() 
class OS {
	static system (init){
    void ?;
  	if ( ! init == ? ) {
    	? = useUpdate() 
      check(>?) >= init ? 
      document.appendChild('shell').parentelement?.removeChild('main') 
      if ( ? == done ) {
      	return ? init ;
      }
    }
	}
	// ? +> init == shell
}
const state = new Internet() 
